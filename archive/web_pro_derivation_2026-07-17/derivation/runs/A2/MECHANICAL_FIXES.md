# A2-r01 机械与无歧义数学修复记录

四个网页下载原件完整保存在 `derivation/runs/A2/raw_downloads/`；规范化候选文件也保留原始内容，未被回写。通过审查的滚动上下文位于 `derivation/modules/A/`。

1. 将接受版文件头的“上下文候选版本”规范为“上下文版本”，并把状态由 `candidate` 改为 `accepted`。
2. 在二维单支持特例中补回库仑摩擦锥的另一侧不等式。原候选正确给出了沿 $+\boldsymbol\tau$ 滑移的上侧载荷比，但“分母非正即水平自锁”的文字未显式说明仍需满足相反锥面的下界。接受版新增

   $$
   \frac{F}{W}\ge
   \max\!\left(0,
   \frac{\tan\phi-\mu}{1+\mu\tan\phi}
   \right),
   $$

   并明确“自锁”只表示规定 $+x$ 扰动没有有限上侧滑移界，不表示任意较小 $F$ 都可行。该修复由候选已采用的完整条件 $|\lambda_t|\le\mu\lambda_n$ 唯一确定，不引入新的物理机理或工程事实。

未修改工程事实候选、运行摘要或引用说明；本轮没有工程事实变化，也没有需要人工取舍的物理分支。
