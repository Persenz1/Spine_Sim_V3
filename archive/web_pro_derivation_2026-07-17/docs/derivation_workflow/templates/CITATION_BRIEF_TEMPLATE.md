# `{MODULE}` 简短引用说明

> 运行：`{A1/...}`
> 提示词版本：`{version}`
> 用途：仅本地归档；不进入任何后续默认上传、交互、模块上下文、集成模型或工程事实审批。

## 1. 关键公式与来源

只保留本轮最关键的少量公式。每个公式后紧跟顺序编码引用；同一公式由多个来源共同支持时使用 `[1,3]`。

例如：

$$
z(x,y)=\sum_{\mathbf q}\sqrt{2C(\mathbf q)\Delta q_x\Delta q_y}\cos(\mathbf q\cdot\mathbf x+\phi_{\mathbf q}).
$$

该式表示由目标二维功率谱密度构造随机高度场的一类谱合成形式；具体归一化取决于离散傅里叶约定，生成后必须回算 PSD 校验 [1,3]。

## 2. 关键方法选择与结论来源

- `{方法或结论}` [2]
- `{由多个来源共同支持的方法或结论}` [1,3]
- `{若属于固定工程约束，写“工程事实：SURFACE.HEIGHT_FIELD.PRIMARY”，不分配参考文献号。}`

## 参考来源

[1] 文献3

[2] https://example.org/direct-source-or-dataset

[3] GPT 自带知识：二维随机场的谱合成、实值场共轭对称和离散 PSD 回算；作为通用数学与数值方法使用，具体归一化和材料参数仍需验证。

## 编写规则

1. 顺序编号按本文件首次出现次序分配，与本地文献自身编号无关。
2. 本地上传文献只写“文献N”，不写标题、作者、压缩包路径或网址。
3. 外部网络资料和公开数据库只列可直接访问的来源网址；不要列搜索结果页。
4. GPT 自带知识必须简述实际使用的知识、假设或适用边界，不得伪造文献或网址。
5. 同一来源全文复用同一编号；未在正文引用的来源不得进入列表。
6. 本文件不复制完整推导，不记录全部工程事实，也不替代 `MODULE_CONTEXT` 中的证据边界。
7. 文件生成后只归档；除非人工在完整机理完成后明确要求，不做跨轮合并或回传。
